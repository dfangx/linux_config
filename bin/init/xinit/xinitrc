#!/bin/bash

autostart="~/bin/init/xinit/autostart"
[ -x "$autostart" ] && . "$autostart"

if [ -d /etc/X11/xinit/xinitrc.d ]; then
    for f in /etc/X11/xinit/xinitrc.d/*; do
        [ -x "$f" ] && . "$f"
    done
    unset f
fi

exec $STARTUP spectrwm

